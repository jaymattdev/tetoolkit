# Text Extraction Tool - Requirements
# All packages are included in Anaconda base installation

pandas>=1.3.0
openpyxl>=3.0.0
toml>=0.10.0
jellyfish>=0.9.0  # For fuzzy string matching (included in Anaconda)

# Optional: For PDF/DOCX support (also included in Anaconda)
# PyPDF2>=2.0.0
# python-docx>=0.8.11
